import 'package:flutter/material.dart';


class SenaraiKenderaan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Senarai Kenderaan'),
      ),
      body: Center(
        child: Text('This is the Senarai Kenderaan page'),
      ),
    );
  }
}
